% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_covariates.R
\name{simulate_gaussian}
\alias{simulate_gaussian}
\title{Simulate Gaussian random fields}
\usage{
simulate_gaussian(
  vgm = NULL,
  psill = 1,
  model = "Sph",
  range = NULL,
  nugget = 0,
  beta = 0,
  nmax = 30,
  indicators = FALSE,
  seed = NULL
)
}
\arguments{
\item{vgm}{A variogram model object (\code{variogramModel} or \code{gstatVariogramModel}).
If \code{NULL}, one is built from \code{model}, \code{psill}, \code{range}, and \code{nugget}.}

\item{psill}{Partial sill (default: 1). Ignored if \code{vgm} is provided.}

\item{model}{Variogram model type (e.g., \code{"Sph"}, \code{"Exp"}, \code{"Gau"}, \code{"Nug"}).
Ignored if \code{vgm} is provided.}

\item{range}{Spatial range parameter. \strong{Required} if \code{vgm} is \code{NULL}.}

\item{nugget}{Nugget effect (default: 0). Ignored if \code{vgm} is provided.}

\item{beta}{Mean of the Gaussian field (default: 0). Passed to \code{gstat::gstat()}.}

\item{nmax}{The number of nearest observations used for kriging simulation (default: 30).
Passed to \code{gstat::gstat()}.}

\item{indicators}{Indicator thresholds (default: \code{FALSE}). Passed to
\code{stats::predict()}. Use \code{TRUE} for default thresholds or a numeric vector
for custom thresholds.}

\item{seed}{Optional random seed for reproducibility. Set this to ensure
that the same random field is generated every time the returned function
is called.}
}
\value{
A function that accepts \code{rast_grid} and \code{n} and returns a \code{SpatRaster}
}
\description{
Returns a function that simulates spatially correlated Gaussian random fields
using a variogram model.
}
\examples{
rast_grid = terra::rast(ncols = 300, nrows = 100,
  xmin = 0, xmax = 300, ymin = 0, ymax = 100)
# Direct usage
sim_fn = simulate_gaussian(range = 25)
sf1 = sim_covariates(rast_grid, n = 4, method = sim_fn)
# With custom variogram
vgm = gstat::vgm(model = "Exp", psill = 1, range = 10)
sim_fn2 = simulate_gaussian(vgm = vgm)
sf2 = sim_covariates(rast_grid, n = 4, method = sim_fn2)
terra::plot(sf1)
terra::plot(sf2)
}
