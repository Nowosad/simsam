% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_proxy.R
\name{proxy_edf}
\alias{proxy_edf}
\title{Euclidean Distance Field proxy factory}
\usage{
proxy_edf(coords = NULL)
}
\arguments{
\item{coords}{Optional coordinates. Can be a matrix, data frame, \code{sf}, or
\code{SpatVector} with x and y columns. If \code{NULL}, uses default locations.}
}
\value{
A function that accepts \code{rast_grid} and returns a \code{SpatRaster}
}
\description{
Returns a function that generates Euclidean Distance Fields from specified
locations (default: four corners and center of the raster extent).
}
\examples{
rast_grid = terra::rast(
  ncols = 300, nrows = 100,
  xmin = 0, xmax = 300,
  ymin = 0, ymax = 100
)

# Default: four corners and center
make_proxy(rast_grid, proxy_edf())

# Custom coordinates
custom_coords = matrix(c(50, 50, 150, 150), ncol = 2, byrow = TRUE)
rownames(custom_coords) = c("point1", "point2")
make_proxy(rast_grid, proxy_edf(coords = custom_coords))
}
