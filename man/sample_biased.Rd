% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sam_field.R
\name{sample_biased}
\alias{sample_biased}
\title{Biased (preferential) sampling method}
\usage{
sample_biased(
  covariate = NULL,
  strength = 1,
  fun = NULL,
  combine = c("prod", "mean"),
  range = NULL,
  replace = FALSE,
  ...
)
}
\arguments{
\item{covariate}{Name(s) or index/indices of the raster layer(s) to use as
the biasing covariate(s). Defaults to all layers of \code{x}.}

\item{strength}{Numeric controlling the strength of the bias applied to the
rescaled covariate values. \code{strength = 0} gives (approximately) uniform
sampling, \code{strength = 1} weights sampling linearly by covariate value, and
larger values give a stronger preference for high-value cells. Negative
values bias towards low-value cells.}

\item{fun}{Optional function applied to the combined, rescaled (\verb{[0, 1]})
covariate raster to produce a weights raster. When supplied it overrides
\code{strength}. Must accept and return a numeric vector (it is passed to
\code{terra::app()}).}

\item{combine}{How to combine multiple covariates into a single weight,
either \code{"prod"} (product) or \code{"mean"}. Ignored for a single covariate.}

\item{range}{Optional named list giving \code{c(min, max)} value ranges used to
restrict sampling to cells whose covariate values fall within the range,
e.g. \code{list(elevation = c(100, 500))}. Cells outside the range are masked out.}

\item{replace}{Logical; should cells be sampled with replacement?}

\item{...}{Reserved for future use.}
}
\value{
A function that accepts \code{x} (SpatRaster) and \code{size} and returns an
\code{sf} object.
}
\description{
Returns a function that performs preferential sampling on a spatial field.
A weights layer is built from one or more covariates (as a \code{SpatRaster}),
and cells are sampled with probability proportional to those weights, so
samples can be biased towards particular parts of the covariate value range.
}
\examples{
rast_grid = terra::rast(
  ncols = 300, nrows = 100,
  xmin = 0, xmax = 300,
  ymin = 0, ymax = 100
)
terra::values(rast_grid) = runif(terra::ncell(rast_grid))

sam_field(rast_grid, 100, method = sample_biased(strength = 2))
}
